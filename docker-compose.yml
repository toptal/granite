services:
  postgresql:
    image: 'postgres:18'
    environment:
      POSTGRES_USER: granite
      POSTGRES_PASSWORD: granite
    volumes:
      - granite_dbdata:/var/lib/postgresql
    ports:
      - '5432:5432'

volumes:
  granite_dbdata:
